import React from "react";
import "./spinner.css";

const Spinner = () => {
  return (
    <div class="spinner-container">
      <div class="spinner"></div>
    </div>
  );
};

export default Spinner;
